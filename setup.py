import setuptools

setuptools.setup(
    name='units_converter',
    version='1.0',
    scripts=['app.py'],
    author='Anna Tran',
    description='A units converter package',
    packages=setuptools.find_packages()
)